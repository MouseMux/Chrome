UPDATE v21 - keyboard through the window (branch keyboard-aura-routing)
=======================================================================

Started 2026-09-03. One entry per change, in order, each with the commit in the
Chromium tree (chrome-151-20260809/src) so any step can be reverted alone.
Build stamp #61 / 2.2.61 unless stated.

Every entry: WHAT changed, WHY (what was measured), and HOW TO REVERT.


1. Keyboard routed through the window            src commit 8cb777fd52e (snapshot)
----------------------------------------------------------------------------
What:   MOUSEMUX_AURA_KEYBOARD (mouse_mux_config.h, on). The controller no
        longer hands keys to the renderer; it posts WM_MOUSEMUX_KEYDOWN /
        KEYUP / CHAR to the user's window. DesktopWindowTreeHostWin::
        PreHandleMSG turns them into ui::KeyEvents flagged EF_IS_SYNTHESIZED
        and sends them where a real WM_KEYDOWN enters (the focus manager), and
        inserts characters straight into the focused TextInputClient.
        RenderWidgetHostViewEventHandler lets synthesized events past native
        keyboard blocking.
Why:    Keys reached only the page. The omnibox, find bar and browser shortcuts
        live in the views focus manager, which the renderer path never visits.
Revert: comment out #define MOUSEMUX_AURA_KEYBOARD; the old renderer path is
        the #else branch of InjectKeyboardEvent and still compiles.

2. OS-inactive window re-activated on the views side only     (same snapshot)
----------------------------------------------------------------------------
What:   Before dispatching a synthetic key to a window the OS has deactivated,
        PreHandleMSG runs HandleActivationChanged(true). During a synthetic
        key IsActive() reports true, Activate() and ClearNativeFocus() do
        nothing (g_mousemux_synthetic_key).
Why:    Measured with a foreground-window recorder: the OS foreground flipped
        between two users' windows on every keystroke. Stack traces showed the
        focus manager activating the widget (SetWindowPos) before it would
        focus a view in an inactive window, and View::OnFocus calling
        SetFocus(hwnd). With the overrides: zero activations in 669 keys.
Revert: remove the three g_mousemux_synthetic_key checks in
        desktop_window_tree_host_win.cc.

3. No OLE drag-and-drop from an injected mouse                 (same snapshot)
----------------------------------------------------------------------------
What:   g_mousemux_sdk_button_held (set by the controller while any injected
        mouse holds a button). WebContentsViewAura::StartDragging and
        DesktopDragDropClientWin::StartDragAndDrop refuse to start an OLE drag
        while it is set.
Why:    Field dump 2026-09-04 (other machine, "Chrome hangs"): UI thread in
        ole32!DoDragDrop under StartDragging. DoDragDrop ends only on a real
        button release; a captured device never produces one.
Cost:   an injected mouse cannot drag links, images or selected text out of
        a page or onto the bookmark bar. Selection itself is unaffected.
Revert: remove the two g_mousemux_sdk_button_held checks.

4. Chrome UI under an injected mouse                            (same snapshot)
----------------------------------------------------------------------------
What:   (a) SetForegroundWindow on synthetic clicks commented out in
        PreHandleMSG. (b) Mouse moves over non-page areas posted as
        WM_MOUSEMUX_MOUSEMOVE (about 60 Hz), WM_MOUSEMUX_MOUSELEAVE when the
        pointer leaves that window or returns to the page. (c) Before injecting
        into a page, an aura hit test (PageIsTopmostAt) checks that the page
        is what is on top; a child widget such as the find bar takes the
        window path instead.
Why:    (a) trace: menu_before=1, menu_after=0 on every menu click - activating
        the menu popup deactivated its owner and closed the menu. (b) menus and
        toolbar buttons never saw hover. (c) clicks on the find bar's X were
        injected into the page underneath.
Revert: (a) uncomment; (b) drop the kMouseMove case from the two dispatch
        conditions in InjectMouseEventToAnyView; (c) remove the PageIsTopmostAt
        call.

5. Virtual cursor                                               (same snapshot)
----------------------------------------------------------------------------
What:   g_mousemux_virtual_cursor_* (defined in ui/display/win/screen_win.cc,
        set by the controller on every injected mouse event, valid while
        anybody owns a window). ScreenWin::GetCursorScreenPoint() and aura's
        GetLastMouseLocationInRoot() (synthesized moves on window changes)
        answer with the injected pointer's position.
Why:    Chrome's menu asks "where is the mouse" when a submenu opens and
        re-selects from the answer; the real mouse idles elsewhere.
Note:   this alone did not stop the flicker; see 6.
Revert: remove the two reads; the definitions can stay.

6. Native moves and wheel blocked in Chrome UI while blocking   src dd46eb45a01
----------------------------------------------------------------------------
What:   The views-layer native block (MOUSEMUX_FULL_NATIVE_BLOCK) now drops
        WM_MOUSEMOVE, WM_MOUSEWHEEL and WM_MOUSEHWHEEL as well as the button
        messages. Dialog and help window remain exempt.
Why:    MOUSEMUX_MENU_TRACE stack traces: every deselect came from a NATIVE
        WM_MOUSEMOVE (HWNDMessageHandler::HandleMouseEventInternal) reaching
        the menu window, interleaved one-for-one with our moves. Windows
        re-posts an idle mouse's position to the window holding capture.
Cost:   while "Block native mouse input" is on, the operator's real mouse
        gets no hover in Chrome UI (it could not click there anyway).
Revert: revert commit dd46eb45a01.

7. Ownership routes the mouse                                   src 513d243572d
----------------------------------------------------------------------------
What:   A user who owns a window sends every mouse event to that window.
        Inside it, the point picks one of its own popups (its menu), its
        page, or its Chrome UI. Outside it, moves and releases still reach
        the page as under Windows capture; presses are dropped. The search
        over all windows by position (Z-order walk) survives only for a user
        who owns nothing yet.
Why:    Two overlapping windows: a click on the front window's toolbar was
        injected into the page of the window behind it (its rectangle
        contained the point; toolbars do not compete in a page search).
        Position-first routing predates ownership.
Cost:   switching windows is only possible via Release in the dialog; the
        "Keep each user in their own window" checkbox is now redundant and
        will be removed in a later step.
Revert: revert the commit alone.

8. Dialog always clickable; wheel follows ownership             src 60bce070bbb
----------------------------------------------------------------------------
What:   The MouseMux dialog and its help window are checked before the
        ownership rule: any user can click them whatever window they own.
        The wheel goes to the owned window's page wherever the pointer is,
        the same rule as clicks. Debug-only OWNED ROUTE log line per press.
Why:    Entry 7 dropped presses outside the owned window, and the dialog is
        outside it - nobody could release or toggle anything. The wheel was
        still picking a page by position, inconsistent with the rule.
Revert: revert the commit alone (loses both).

9. Users list: "Window N · title"                              src 5c507e1cc5d
----------------------------------------------------------------------------
What:   The row's window column leads with the window's number in the order
        the windows were opened, then the page title. The screen is gone.
Why:    two windows on the same page read identically (the title is the
        active tab's); the screen did not tell them apart.
Revert: revert the commit alone.

10. Screen pixels through Chrome's per-monitor mapping          src fe28a86e981
----------------------------------------------------------------------------
What:   Every MouseMux screen position is converted with
        display::win::ScreenWin::ScreenToDIPPoint (page injection, wheel,
        the unclaimed-user hit test, the owned-window page check) instead of
        pixels / this window's scale factor. PageIsTopmostAt no longer
        subtracts the window origin twice.
Why:    Customer screenshot 2026-09-03: pointer far left of a menu the page
        kept hovering. Chrome lays monitors out edge to edge in scaled units;
        on a monitor whose scaling differs from the primary's, the division
        lands a few hundred units off, so hover, clicks and the wheel act at
        the wrong spot. Right with one monitor or uniform scaling - which is
        why it appeared only with a third user on a third monitor.
Revert: revert the commit alone.

11. MOUSEMUX_ANONYMOUS_KEYS; stamp #62 / 2.2.62                 src d3fc4db9870
----------------------------------------------------------------------------
What:   With MOUSEMUX_DEBUG on, keys that produce text are logged as 'X'
        (vkey 0x58, scan 0, character U+0058) and raw MouseMux keyboard
        messages are not logged. Modifier, navigation and function keys
        are logged as they are. Comment the define out in
        mouse_mux_config.h for rich logging on the bench.
Why:    a debug build sent to a customer must not come back with a log
        that reads as what their users typed.
Package: chrome-release-2.2.62-debug-20260904 (field test for Gustavo).
Revert: comment out #define MOUSEMUX_ANONYMOUS_KEYS.

12. Logging that follows every event end to end                 src a46c70d7e14
----------------------------------------------------------------------------
What:   Under MOUSEMUX_DEBUG, four line kinds in mousemux_debug.log:
          IN     what arrived (device, user, raw screen pixels)
          ROUTE  the branch taken and why (owned window, popup, page,
                 Chrome UI, outside, drag lock, unowned search)
          OUT    what was handed to Chrome (view, host, scaled coordinates,
                 view bounds, widget coordinates, pending/ignoring state,
                 cross-process frame count)
          ACK    Chrome's answer to every input event of every registered
                 view (RenderWidgetHost::InputEventObserver): consumed,
                 not-consumed, NO-CONSUMER, IGNORED; ours=1 for injected
                 events (kFromDebugger), ours=0 for native
        Mouse moves every 50th per device (acks every 50th per host);
        everything else always. Keys carry user and window.
Why:    Gustavo's 2.2.62 logs showed every wheel event handed to the page
        and nothing about what the page did with it. The next field log
        must name the outcome.
Revert: revert the commit alone; no behaviour is involved.

13. A user owns a set of windows; one window, one owner         src 30b0a9c74ce
----------------------------------------------------------------------------
What:   Each user has a set of owned windows and one current window; all
        their input goes to the current one. A press inside another window
        of their own set makes it current (rectangle test against this
        user's windows only). A new window is adopted by its opener's owner
        (popups, links into new windows, print), else by the user whose
        injected input arrived last within 2 s (Ctrl+N, the menu); with no
        recent injected input it stays unowned. A window in somebody's set
        cannot be claimed by anyone else. A closed window leaves the set;
        the user is released only when the set is empty; Release hands back
        the whole set. Dialog row shows "+N" for extra windows.
Why:    a window the owner opened themselves (Ctrl+N, a site's login popup)
        could not be used: presses outside the single owned window were
        dropped. And two users could end up owning one window.
Revert: revert the commit alone.

14. The wheel's scroll gesture is ended                          src f4083facb85
----------------------------------------------------------------------------
What:   Per device, 100 ms after the last wheel notch, a zero-delta wheel
        event with phase Ended goes to the page the last notch went to
        (SendWheelEnd), as MouseWheelPhaseHandler does for a real mouse.
Why:    Every injected wheel was phase Began and nothing ever ended the
        gesture, so Chrome kept scrolling whatever the first notch latched
        onto - nothing, when that notch hit a spot without a scroller or
        the end of one. ACK log 2026-09-04: 206 consecutive "no consumer"
        on a page a real mouse scrolled at once; 447 scroll updates, 3
        scroll begins. This is the customer's "their wheel stops until I
        scroll there with my mouse".
Revert: revert the commit alone.

15. "[N] title" in every window caption                          src 33a055b3a3a
----------------------------------------------------------------------------
What:   A stable number per browser window (assigned when first seen, kept
        while it lives, keyed by session id) prefixed to the caption while
        MouseMux integration is on, and used by the dialog's "Window N".
        Header-only helper mouse_mux_window_number.h.
Why:    a person at a seat could not tell which window their row meant; and
        the dialog's numbering shifted when a window closed.
Revert: revert the commit alone.

16. "[Green] title": the owner's name in the caption              src 6d5464b02d8
----------------------------------------------------------------------------
What:   A window's caption starts with its owner's MouseMux name; with the
        window number while nobody owns it. Captions refresh on every
        ownership change, including a window joining a user's set.
Why:    the person at a seat had no way to see on the window that it was
        theirs (entry 15's number was not what was asked for).
Revert: revert the commit alone.

17. The window under a press is what Windows says it is           src eae7751eea4
----------------------------------------------------------------------------
What:   WindowFromPoint names the top-level window under a press (a menu
        popup resolves to the browser window that owns it). Rule: the
        dialog/help window get the press whoever the user is; one of the
        user's own windows becomes current and gets it; an unowned window
        is claimed; anything else is dropped. Moves/wheel/keys go to the
        current window without a lookup; a move over the current window or
        its popups is delivered there for hover. Replaces three rectangle
        tests.
Why:    rectangles cannot know what is on top: two overlapping windows of
        one user alternated on every press, so each click took two.
Revert: revert the commit alone.

18. Owner chip in the toolbar                                   src 57a28dd7352
----------------------------------------------------------------------------
What:   A label before the "..." button with the owner's status dot and
        MouseMux name ("● Green"), hidden while nobody owns the window.
        Set by the dialog on every ownership change, next to the caption.
Why:    the caption only shows in the taskbar with Chrome's own frame; the
        person at the seat wants to see it on the window.
Revert: revert the commit alone.

19. Injected Chrome-UI events dropped where the hit test is not client   src 4a8fd71a5b2
----------------------------------------------------------------------------
What:   In the views hook, the frame's own NonClientHitTest decides: any
        answer but HTCLIENT (caption buttons, drag strip, resize edges)
        drops the injected press or move. Tabs, new-tab button, toolbar,
        chip and page answer HTCLIENT and are unaffected.
Why:    an injected click on minimize/maximize/close reached Chrome's
        painted caption buttons as an ordinary press: a user could close a
        window that was theirs to work in, not to manage; and the buttons
        flickered under an injected pointer.
Revert: revert the commit alone.

20. "Keep each user in their own window" removed                 src dcec6690bf9
----------------------------------------------------------------------------
What:   The checkbox, its help entry, the toggle handler, SetHardLock /
        IsHardLock, hard_lock_, locked_window and the two dead branches on
        the first-claim path are gone. README no longer lists it.
Why:    since entry 7 every user is always kept in their own windows; the
        option changed nothing.
Revert: revert the commit alone (restores a checkbox that still does nothing).

21. Dialog footer (Help, Collapse, build label) in every build   src 21a05e091ac
----------------------------------------------------------------------------
What:   The footer is no longer wrapped in #ifndef MOUSEMUX_DEBUG.
Why:    leftover from the old debug log panel in that spot; a debug build had
        neither panel nor footer, so no Help and no Collapse. Debug and
        release dialogs are identical now.
Revert: revert the commit alone.

22. Native blocking per owner                                    src 9dc6239b2f4
----------------------------------------------------------------------------
What:   Every window an owner holds drops native mouse and keyboard input
        (page event handlers, and the views hook now checks a set of
        blocked HWNDs), unless the owner's row checkbox "Block native" is
        unticked. Unowned windows stay native for the operator. The global
        checkbox is gone; the control server's everything-at-once flag stays
        as a legacy fallback.
Why:    "block native" is per window in practice: the owner's own device is
        captured and silent, so what is dropped is other people's real
        mice, and only where somebody works.
Revert: revert the commit alone.

23. Logging for frozen-input situations                          src c7b46531ea6
----------------------------------------------------------------------------
What:   NATIVE/In in chrome-log.txt: every native mouse/key message that
        reaches a Chrome window, position, and the decision (BLOCKED-here,
        passes, exempt dialog/help); moves every 50th. BLOCKING in
        mousemux_debug.log: the blocked-window set with owners and each
        view's state whenever recomputed. FWD: every event forwarded to a
        page with its origin - injected-page, injected-ui, NATIVE - so the
        ACK after it can be attributed (the old ours= mark could not tell
        injected-ui from native).
Why:    "everything froze" on the bench (2026-09-04 20:31) could not be read
        from the logs: native input arriving nowhere is silent.
Revert: revert the commit alone; no behaviour is involved.

24. Blocking recomputed once a new tab's view has its window      src 141047a7d46
----------------------------------------------------------------------------
What:   The deferred lookup that adopts new windows (MaybeAdoptNewWindow)
        also recomputes native blocking when the view's window is known.
Why:    a new tab's view registers before it is attached, so RegisterView
        computed blocked=0 for window=(nil); the second tab of a blocked
        window then took the real mouse's wheel and clicks (log 20:41).
Revert: revert the commit alone.

25. Double-click and side-button messages blocked; all input classes logged  src 76912189950
----------------------------------------------------------------------------
What:   WM_*BUTTONDBLCLK and WM_XBUTTON* added to the native block list.
        NATIVE/In now logs mouse, key, pointer (pen/touch/mouse-as-pointer),
        touch, gesture and raw-input messages, saying whether the window is
        in the blocked set; the actual drop is the BLOCKED line after it.
Why:    a fast second press is WM_LBUTTONDBLCLK, which the list did not
        contain: 16 of them reached a blocked window's tab strip and switched
        tabs while all 36 single presses were dropped (20:52). The previous
        NATIVE/In line printed a predicted decision, wrong for exactly those.
Revert: revert the commit alone.

26. Page focus trace                                            src d4b401ad58c
----------------------------------------------------------------------------
What:   PAGE/Focus in chrome-log.txt: page view, window, GAINED/LOST,
        blur-suppressed flag, stack trace. MOUSEMUX_DEBUG_TRACE only.
Why:    the caret in a text field disappears under blocked native clicks
        (2026-09-04, no button message reached any page); the cause must
        pass through the page's focus change, which was silent.
Revert: revert the commit alone.

27. A native click on a blocked window no longer activates it    src f8a7743fb85
----------------------------------------------------------------------------
What:   WM_MOUSEACTIVATE for client-area clicks on a blocked window is
        answered MA_NOACTIVATEANDEAT, in the frame hook and in the per-page
        child window (which Windows asks first and which never forwards the
        question). Caption buttons and edges unchanged.
Why:    the activation request precedes the button message and was never
        blocked: the click was dropped but the window came to the front,
        the previously active window deactivated and Chrome blurred its
        page - the caret vanishing under a blocked click (13 blurs in 3
        minutes at 21:12, zero clicks delivered).
Revert: revert the commit alone.

28. The release after a caption-button press passes the block    src bf80464d474
----------------------------------------------------------------------------
What:   A window whose caption button was pressed by the real mouse is
        remembered; the next client-area button-up on it passes the block.
Why:    Chrome's frame handles caption buttons itself and waits for the
        release, which arrives as a client button-up that the block dropped:
        the button stayed pressed, the frame kept the mouse, the window
        looked dead until an activation change (21:22, UI thread idle).
Revert: revert the commit alone.

29. An owned window keeps its page focus across activation changes   src 6e0fde49a16
----------------------------------------------------------------------------
What:   ShouldSuppressBlur is true for a page in an owned window, captured
        or not (was capture-gated). Unowned windows unchanged.
Why:    with nobody captured, every OS activation change blurred the
        deactivated window's page and killed a caret somebody was typing
        behind (21:12: thirteen blurs in three minutes). Under ownership
        routing, activation says nothing about who is working where.
Revert: revert the commit alone.

30. Users list: window after the name, column lines, header row   src 8e0b1c9f73a
----------------------------------------------------------------------------
What:   One table definition for a header row and every user row: dot |
        User || Window || Keyboard || Typing || Capture | Block native |
        Release | x, one-pixel lines between the text columns. Dialog
        width 660 -> 740 so the window title keeps about 190px.
        Follow-up 5a543ef3f2d: no lines in the header row (they looked like a
        fence); the rows keep theirs.
Why:    the window is what the operator reads first and sat behind two
        narrow status columns; the rows read better as a table.
Revert: revert the commit alone.

Code review follow-ups (2026-09-04/05).  A read-only review of the whole
integration listed 25 findings; each was checked against the code first and
fixed in its own commit where it held.  Two structural items (a shared header
for the cross-layer globals in ui/base; moving the PreHandleMSG blocks into a
MouseMux-owned file) are not done - separate branch.  The debug switches stay
ON in this branch for the bench; off before any release.

31. The dialog unregisters its callbacks before it dies   src 822745bb4d3
----------------------------------------------------------------------------
What:   MouseMuxInputController::ClearUiCallbacks(), called from the dialog
        destructor, resets the ten callbacks the dialog bound to itself.
Why:    closing the dialog ends the seat, so the dialog died before the browser
        windows, and every tab closing afterwards notified ownership changes
        into freed memory.
Revert: revert the commit alone.

32. A closing window releases its owner   src 0dd88bd7820
----------------------------------------------------------------------------
What:   ForgetViewEverywhere drops the closing window from the owned set
        before pruning; the injector's prune on a dead handle releases too.
Why:    Chrome destroys a window's pages before its frame, so IsWindow still
        said yes and the owner was kept on an empty frame.
Revert: revert the commit alone.

33. Claiming uses the window rule   src d3c1ee13d1a
----------------------------------------------------------------------------
What:   OnMouseButton claims through WindowFromPoint like the routing does;
        the rectangle test and its claim-on-miss fallback are gone.
Why:    a press on the desktop or another user's UI made an owner with no
        window, who then hovered everyone's pages.
Revert: revert the commit alone.

34. Owning nothing gets nothing of another user's   src 7eab73e9361
----------------------------------------------------------------------------
What:   in the unowned search every event (not only presses) stops at another
        user's window; wheel with no page under it is dropped instead of
        scrolling the first registered view.
Why:    hover and wheel from a windowless user leaked into other users' pages.
Revert: revert the commit alone.

35. A press focuses the clicked page only   src dc24ce6f1a3
----------------------------------------------------------------------------
What:   host->Focus() for the clicked page; the every-renderer loop on each
        press and FocusKeyboardTargetView (capture toggle) are gone.
Why:    ownership already holds focus (ShouldSuppressBlur); the loop put a
        caret back in pages whose users were typing in the omnibox.
Revert: revert the commit alone.

36. One truth for capture; disposal order   src 83568b2bf11
----------------------------------------------------------------------------
What:   is_captured_ removed, every reader asks IsCaptured(); OnUserDisposed
        removes the owner before erasing its state.
Why:    the cached flag drifted (a disposed device left it stale); RemoveOwner
        re-created the state OnUserDisposed had just erased.
Revert: revert the commit alone.

37. Stale window handles   src 8edafde3843
----------------------------------------------------------------------------
What:   g_mousemux_help_hwnd cleared when the help window dies; OwnerOfWindow
        skips handles whose window is gone.
Why:    Windows reuses handles: a browser window could inherit the help
        window's blocking exemption, or report an old owner and refuse a
        claim.
Revert: revert the commit alone.

38. Virtual cursor per window   src 72c6ffb43bf
----------------------------------------------------------------------------
What:   screen_win.cc keeps one injected position per top-level window (popups
        resolve to their owner); a window's dispatcher asks for its own,
        GetCursorScreenPoint falls back to the most recent, a window with
        none reports the real mouse.
Why:    one global was overwritten by every device: a menu opened by one user
        got synthesized moves at the other's position, and a fresh window
        was told the cursor sat at 0,0.
Revert: revert the commit alone.

39. One ClaimForPress   src bc060573533
----------------------------------------------------------------------------
What:   the three copies of 'a press adopts this window' in the routing are one
        function with one owner check.
Why:    three places set current window, keyboard target and owned set by hand.
Revert: revert the commit alone.

40. Users list reads the current window   src 81fb8412f02
----------------------------------------------------------------------------
What:   GetOwners takes window, title (Win32 caption) and window count from
        claimed_window; keyboard_target_view is a keyboard fallback only.
Why:    the row came from the last clicked tab: gone when it closed, elsewhere
        after a drag, while the current window was known.
Revert: revert the commit alone.

41. Ownership UI refresh on its own task   src 76fe9820ff2
----------------------------------------------------------------------------
What:   OnOwnershipChanged logs and posts one RefreshOwnershipUi; a second
        notification while one is pending folds into it.
Why:    captions and toolbar chips of every window were updated inline from
        injected input and from a page's destructor, twice per claim.
Revert: revert the commit alone.

42. Native blocking recomputed once per change   src 2e4b7eb81e8
----------------------------------------------------------------------------
What:   SetOwnerBlockNative relies on the notification's apply; RegisterView on
        MaybeAdoptNewWindow's; adoption applies through the notification.
Why:    each of those applied twice.
Revert: revert the commit alone.

43. Server notices in a window, not a MessageBox   src 2d84ecec316
----------------------------------------------------------------------------
What:   NoticeCallback controller -> dialog; MouseMuxNoticeDialog (non-modal,
        Close button) shows timeout warnings and session end.  One friend
        line added in ui/views/window/dialog_delegate.h.
Why:    MessageBoxW inside the socket callback spun a nested loop on the UI
        thread and stalled every user's input until dismissed.
Revert: revert the commit alone.

44. One rounding for SDK positions   src 37bdd44cee8
----------------------------------------------------------------------------
What:   ScreenPixelPoint/ScreenPixelPOINT (floor) replace the mix of
        static_cast<int>/<LONG> and floor across claim, hit tests, window
        lookup, aura dispatch.
Why:    truncation rounds toward zero: on a monitor left of or above the
        primary the same point could be on different sides of an edge.
Revert: revert the commit alone.

45. Adopt retry without a dangling pointer   src 7d30bcd0892
----------------------------------------------------------------------------
What:   views registered before their window exists go into adopt_pending_
        (erased by UnregisterView); RetryPendingAdoptions walks the set.
Why:    a raw view pointer was bound into a task 200 ms out.
Revert: revert the commit alone.

46. MULTI_OWNER and AURA_KEYBOARD promoted   src 5e708542d88
----------------------------------------------------------------------------
What:   the #ifdefs and their #else halves are gone (single-owner claim,
        every-renderer focus loop, SetForegroundWindow, InsertChar keyboard
        path, about 250 lines).  Config header keeps the history.
Why:    shipped and verified, yet the dead halves were still carried.
Revert: revert the commit alone.

47. Dead code out   src dc4a9b0af2a
----------------------------------------------------------------------------
What:   IsPointOverChrome, GetOwnerKeyboardHwid, user_positions_,
        motion_count_, ScreenNumberOf/screen_index, dialog UpdateTitle,
        dialog owner fields, native-blocking-changed callback, ToggleButton
        include; InjectionStats counts posted keys/chars.
Why:    none of it was read, or it described the InsertChar path.
Revert: revert the commit alone.

48. One version stamp; comments match the code   src 5057fce19d7
----------------------------------------------------------------------------
What:   MOUSEMUX_VERSION / BUILD_NUMBER / BUILD_DATE in mouse_mux_config.h,
        used by client and dialog.  ShouldSuppressBlur comments say captured
        OR owned; dialog.h orphan comments removed; prune log names the
        closed window.
Why:    three constants in two files already disagreed on the date.
Revert: revert the commit alone.

49. Menu selection trace removed   src 279bac0f47f
----------------------------------------------------------------------------
What:   MOUSEMUX_MENU_TRACE and its SetSelection block in menu_controller.cc.
Why:    its question was answered on 2026-09-04; it wrote a stack trace to disk
        on every selection change since.
Revert: revert the commit alone.
50. Native block covers the keyboard   src 2b677e9a186
----------------------------------------------------------------------------
What:   PreHandleMSG drops WM_KEYDOWN/KEYUP/CHAR/DEADCHAR/UNICHAR and the
        WM_SYSKEY family for a window on the block list.  Our own
        WM_MOUSEMUX_KEY* messages pass.
Why:    the page dropped native keys (its handler checks the flag) but the
        address bar, a views text field, took them.  With MouseMux in
        switched mode WITHOUT multi-keyboard the server let the keyboard's
        native input through too, and every character typed in the address
        bar arrived twice (13:07: 12 native key-downs beside 24 SDK keys;
        with multi-keyboard on, 0 beside 58).
Revert: revert the commit alone.

51. Virtual cursor: no position of its own -> the latest one   src 662546ead48
----------------------------------------------------------------------------
What:   MouseMuxVirtualCursorAtDIP answers with the most recent injected
        position when the asking window has none; the real mouse only when
        nothing has been injected.
Why:    after entry 38 the ... menu and right-click menus flickered again:
        a popup whose handle did not resolve to its window found no entry and
        fell back to the real mouse, the exact cause fixed on 2026-09-04.
Revert: revert the commit alone (entry 38 then has the bug again).

52. Native block covers a blocked window's popups   src 3146c9d8e6a
----------------------------------------------------------------------------
What:   MouseMuxWindowBlocked(hwnd): on the list, or its GA_ROOTOWNER is.
        Used by the PreHandleMSG block and the NATIVE/In trace.
Why:    the flicker of entry 6 came back with per-owner blocking: the list
        named the browser windows only, menu popups are windows of their own,
        and native WM_MOUSEMOVE reached them again (934 under menus in one
        session, every popup logged "in-open-window").  Entry 51 was aimed
        at the wrong cause and is harmless.
Revert: revert the commit alone.

53. An injected click in the page gives the page Chrome's focus   src 6a14a728b57
----------------------------------------------------------------------------
What:   on an injected page press the controller also calls view->Focus()
        (the page's aura window) with g_mousemux_synthetic_key set, which
        now lives in namespace content.  The views side then neither
        activates the OS window nor moves native focus.
Why:    a real click focuses the page's window on its way through aura; an
        injected one went straight to the renderer.  After a tab closed,
        Chrome restores focus only on activation, which an owned window
        never gets: keys posted to the window found no focused view and
        were dropped (15:11: 114 posted, none forwarded to any page).
Revert: revert the commit alone.

54. KEY/In trace (views)   src 9a29438f2ed
----------------------------------------------------------------------------
What:   under MOUSEMUX_DEBUG_TRACE, every posted key logs window, active
        aura window, focused aura window and focused view class.
Why:    "keys arrive, nothing happens" is this line.
Revert: revert the commit alone.

55. A keyboard whose pairing owns nothing re-requests the user list   src 48be028b46b
----------------------------------------------------------------------------
What:   in the "KEY NOT AN OWNER" branch, RequestUserList() at most every
        two seconds (the existing throttle member).
Why:    the server sent the user list at connect and nothing when a
        keyboard was moved from Lime to Aqua during the session; 128 keys
        kept arriving as Lime's and were dropped.
Revert: revert the commit alone.

56. A menu belongs to whoever opened it, whichever way   src abfda9d79a9
----------------------------------------------------------------------------
What:   MenuDismissCallback(hwid, page_press).  Every injected press feeds
        the attribution before routing; only a page press by the menu's
        owner cancels it.
Why:    the ... button is Chrome UI and never reached the page path, so
        Green's menu was attributed to the last page-presser (Aqua) and
        Aqua's next click in her own page closed it (15:30).
Revert: revert the commit alone.

57. Another user's injected keys are not the menu's   src 3defeb89943
----------------------------------------------------------------------------
What:   menu_pre_target_handler_aura.cc: a synthesized key whose target
        root is not the menu owner's root is ignored by the menu handler.
Why:    the handler is on the process-wide Env and took every key in the
        process while a menu was open; Green's arrows drove and closed
        Aqua's menu (15:40).
Note:   another user's click on Chrome UI (not a page) still closes an
        open menu: it takes mouse capture, and Windows has one per thread.
        Documented limitation until capture knows about users.
Revert: revert the commit alone.

58. A key in another window is not the menu's, native or injected   src 1c1e066077c
----------------------------------------------------------------------------
What:   entry 57 without the synthesized-only condition: any key whose
        target root differs from the menu owner's root is ignored by the
        menu handler.
Why:    with Multi-keyboard off the native copy of a keystroke lands in the
        foreground window - our exempt dialog - and the menu took it from
        there (15:47: six native key-downs at the dialog, one for one with
        Aqua's typing, drove Green's menu).  Block native never covered the
        dialog, by design.
Revert: revert the commit alone (entry 57 remains).

59. user.changed action "update" refreshes the user list   src f41a0dda648
----------------------------------------------------------------------------
What:   mouse_mux_client.cc treats action "update" like "map": request the
        user list; OnUserList rebuilds the pairing from scratch.
Why:    the server (2026-09-05) now sends "update" when a device is
        detached from a user that still exists - unplug, unmap, the losing
        side of a keyboard reassignment.  Previously that action was
        defined but never sent; the client logged it as unhandled, and the
        losing user kept the keyboard in the cached pairing.
Revert: revert the commit alone; entry 55's re-request still covers it,
        two seconds late.

60. AssignWindow(name, window, captured, block_native)   src 6ed641b14a7
----------------------------------------------------------------------------
What:   controller API: the user list maps the MouseMux name to a device,
        AddOwner + ClaimForPress make the window theirs, the two flags are
        applied.  False (nothing changed) when the name is not connected
        or the window is another user's.
Why:    the first building block of save/load and of reconnect memory: a
        window made somebody's without a press.  Names, because device ids
        change with hardware.
Revert: revert the commit alone (nothing calls it yet).

61. Save layout   src 6eb9bbc914b
----------------------------------------------------------------------------
What:   footer button; writes <profile>/mousemux-layout.json: per normal
        window (window-number order) x/y/width/height, maximized, url of the
        active tab, owner, captured, block_native.  Version 1.
Why:    first half of save/load; the file is also what reconnect memory
        will read.
Revert: revert the commit alone.

62. OpenWindowCopy()   src 1827ffd4319
----------------------------------------------------------------------------
What:   "+ Window" as a function returning the new window (found by
        elimination against the windows that existed before).
Why:    Load layout opens windows the same way and then needs the window.
Revert: revert the commit alone.

63. Load layout   src 4175cf09ab6
----------------------------------------------------------------------------
What:   footer button; reads the layout file off the UI thread; entries go
        to the existing normal windows in window-number order, the rest are
        opened as copies of the signed-in tab; bounds, navigation to the
        saved page (in the copy, so session storage survives), then
        AssignWindow by name with the saved flags.
Why:    second half of save/load.  Names not connected yet are skipped and
        logged; those users claim by clicking.
Revert: revert the commit alone.

64. Load layout only while connected   src 83c2652958a
----------------------------------------------------------------------------
What:   the Load layout button is greyed out until connected, like + Window.
Why:    owners are assigned by MouseMux name through the user list, which
        only the connection provides; a disconnected Load rebuilt the
        windows and assigned nobody (17:49: six presses, 0 assigned).
        Simpler than a pending list.
Revert: revert the commit alone.

65. Save layout only while connected   src b1e4963d02b
----------------------------------------------------------------------------
What:   the Save layout button is greyed out until connected.
Why:    disconnected there are no owners; a save then overwrote a layout
        that had them with one that did not.
Revert: revert the commit alone.

66. Control server: capture message (hosted-browser spike, part 1)   src 6a84367be49
----------------------------------------------------------------------------
What:   {"type":"capture","owner":"Green"|"hwnd":N,"page":"<url>"} ->
        DesktopMediaID::RegisterNativeWindow(root) + DesktopStreamsRegistry
        ::RegisterStream for the tab showing <page>; reply
        {"type":"capture","id","hwnd","x","y","width","height"}.  Spike page:
        O:/mousemux-builds/Chrome/spike/capture-host.html.
Why:    getUserMedia(chromeMediaSource "desktop", that id) is served by the
        compositor-based AuraWindowVideoCaptureDevice on Windows: no screen
        involved, and WindowTreeHost::VideoCaptureLock keeps an occluded
        window's compositor alive.  Screen Matrix already requests sources
        exactly this way.
Revert: revert the commit alone.

67. Capture stream bound to process and origin, not one frame   src bb756c7e603
----------------------------------------------------------------------------
What:   RegisterStream with restrict_to_render_frame_id = nullopt; the
        spike page adds a random fragment to its URL before connecting.
Why:    second attempt failed "Error starting tab capture"
        (STREAM_NOT_FOUND_IN_REGISTRY): a second tab or a reload made the
        requesting frame differ from the one the URL lookup found.  The
        first attempt had worked (it captured the spike page's own window,
        which was Green's current one).
Revert: revert the commit alone.


Diagnostics in this branch (all off in a release build):
  MOUSEMUX_DEBUG, MOUSEMUX_DEBUG_TRACE (mouse_mux_config.h and, mirrored,
  desktop_window_tree_host_win.cc): logs in %TEMP%: mousemux_debug.log,
  mousemux_diag.log, chrome-log.txt (activation stack traces, custom-mouse
  trace, native-move-under-menu trace).

Open:
  - flicker after 6: to be tested.
  - "warm-up" of clicks on the other machine: not reproduced; log shows early
    clicks going down the Chrome-UI path with no outcome recorded at the time.
  - keyboard "duplication"/"zero keys" from the other machine: not seen in
    its log (88 down / 89 up, all posted).
